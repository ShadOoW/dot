{
  "description": "The ~/.config/secrets credential store — templates, plus the configure that keeps the directory 700, every file 600 and no symlinks",
  "packages": {},
  "tags": ["secrets"],
  "os": ["linux"]
}
