# Real file: ~/.config/secrets/agent-web, mode 600. Never the .example suffix, never a symlink.
# Consumed by the web-verify skill as: set -a; . ~/.config/secrets/agent-web; set +a
# One pair of vars per site; reference their NAMES from ~/.config/agent-web/sites.json.

BRUCE_LOCAL_USER=
BRUCE_LOCAL_PASS=
